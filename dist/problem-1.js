"use strict";
{
    //
    const sumArray = (numbers) => {
        let sum = 0;
        numbers.map((value) => sum + value);
        return sum;
    };
    // ;
    console.log(sumArray([1, 2, 3, 4, 5]));
    //
}
